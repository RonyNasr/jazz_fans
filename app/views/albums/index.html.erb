<div id="albums">
  <h1>Albums</h1>

  <% @albums.each do |album| %>
    <div class="album">
      <h5><%= link_to album.name,  album_path(album)%></h5>
    </div>
  <% end %>

</div>

<p>
  <%= link_to "Add Album", new_album_path %>
</p>
