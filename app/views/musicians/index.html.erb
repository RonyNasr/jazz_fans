<div id="musicians">
  <h1>Musicians</h1>

  <% @musicians.each do |musician| %>
    <div class="musician">
      <h5><%= link_to musician.name,  musician_path(musician)%></h5>
    </div>
  <% end %>

</div>

<p>
  <%= link_to "Add a Musician", new_musician_path %>
</p>
